FROM python:3.12-slim

WORKDIR /app

# Install dependencies
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Copy application code
COPY . .

# Background workers don't need ports, but keeping it for compatibility
ENV PORT 8080

CMD ["python", "app/workers/scheduler.py"]
